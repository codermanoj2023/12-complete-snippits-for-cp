{
    "used to convert string into int hash": {
		"prefix": "get_hash",
		"body": [
			"int get_hash(vi &pref, vi &base_pow, int l ,int r){",
			"    int h = pref[r + 1] - (base_pow[r - l + 1] * pref[l] % MOD) % MOD;",
			"    return h < 0 ? h + MOD : h;",
			"}"
		],
		"description": "used to convert string into int hash"
	}
}